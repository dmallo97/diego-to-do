import React from 'react';

import { View, Text } from 'react-native';

const AddListItemModal = () => {
  const sampleText = 'Welcome to Add List Item Modal';
  return (
    <View>
      <Text>{sampleText}</Text>
    </View>
  );
};

export default AddListItemModal;
